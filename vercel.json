{
  "installCommand": "if cd flutter; then git pull && cd .. ; else git clone -b stable https://github.com/flutter/flutter.git; fi && flutter/bin/flutter config --enable-web",
  "buildCommand": "flutter/bin/flutter build web --release --dart-define=API_KEY=$API_KEY",
  "outputDirectory": "build/web",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
